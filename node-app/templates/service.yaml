apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-ap.fullname" . }}
  labels:
    {{- include "node-ap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-ap.selectorLabels" . | nindent 4 }}
