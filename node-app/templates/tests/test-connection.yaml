apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "node-ap.fullname" . }}-test-connection"
  labels:
    {{ include "nodeapp.fullname" . }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "node-ap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
